# Claude Agent Guide

This repository keeps a **single** agent guide so the two entry points can never
drift. The canonical content — start-here order, invariants, project map,
commands, and links — lives in **[AGENTS.md](./AGENTS.md)**.

Claude Code auto-loads this file, so its only job is to send you there:

➡️ **Read [AGENTS.md](./AGENTS.md) first.** Everything an agent needs is there or
linked from it.
